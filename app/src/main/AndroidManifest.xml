<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="dev.jamescullimore.trustkeyservice.permission.ACCESS_KEYSTORE_SERVICE"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrustKeyService">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TrustKeyService">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.KeystoreCommandService"
            android:enabled="true"
            android:exported="true"
            android:permission="dev.jamescullimore.trustkeyservice.permission.ACCESS_KEYSTORE_SERVICE">
            <intent-filter>
                <action android:name="dev.jamescullimore.trustkeyservice.action.BIND_KEYSTORE_SERVICE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".service.KeystoreCommandReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP">
            <intent-filter>
                <action android:name="dev.jamescullimore.trustkeyservice.action.EXECUTE_COMMAND" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
